# CSV Processor 開発計画

対象: `241217_csv_processer`（index.html / csvProcessor.js / csvProcessor.css）
作成日: 2026-08-19（最終更新: 2026-09-18）
本ドキュメントは**未対応の残タスクのみ**を管理します。対応が完了した項目・対応不要/対応方針を決定した項目・調査の結果問題なしと確認できた項目（1-1〜1-5、2-1、2-2、2-3、2-4、2-5、BOM対応、うっかり欠損バグ修正、プレビューのレイアウト崩れ、tooltipのレイアウト崩れ、ディレクトリの書き出し、強制的な処理の打ち切り、複雑なCSVの読み込み確認、列数が合わない行の扱い、コードクリーンアップ一式等）は [開発記録.md](./開発記録.md) に移動しました。項番は開発記録.md と通しになっています。

---

## 1. 開発者コメントに記載の既知の問題（残っているもの）

| 項目 | 現状 | 備考 |
|---|---|---|
| プロファイル読込・書き出しの改善・削除機能 | 削除機能は実装済み | `removeProfile`（1887行付近）が既に存在。コメントが古い可能性が高く、要更新。読込・書き出し自体の「改善」内容は要ヒアリング |

---

## 2. UI上ですでに「未対応」と明示されている機能（レビュー対象外・現状維持でOK）

- 出力ファイル存在時の上書き/追記の選択（`outputFileExistActionRadiobutton`、disabled済み）

---

## 3. 開発計画（フェーズ案・残タスクのみ）

### 保留（当面着手しない）
- UIの折りたたみ対応（設定パネルの折りたたみUI）

### フェーズ4：未実装機能の実装（優先度は要相談）
- 文字コード自動判定の精度検証・テストケース整備（2-5対応後、実データでの検証が未実施）
- `directoryReloadButton`（フォルダ再読込ボタン）へのクリックイベント実装。現状クリックイベントが一切登録されていない死んだボタン
- 読み込みモード2の実装（当面は見送り。大容量CSVで「セル内に区切り文字を含む」設定時の処理速度が実運用で問題になった場合に再検討。詳細は開発記録.md 2-4参照）

---

## 4. コード内の★マーク一覧（開発者本人による「微妙・未完成」の目印、残っているもの）

コード中の `★` は、開発者自身が「微妙だと思った箇所」「未完成の箇所」を強調するために付けたマーク。旧★-1〜★-3（`csvProcessor`をユーザーコードに渡す件）は対応不要と判断、旧★-2（`readingModeView`）は削除方針が確定したため、いずれもコードから★を除去し、この一覧からも削除（詳細は開発記録.md参照）。

| # | 該当箇所 | 内容 | 対応状況・方針 |
|---|---|---|---|
| ★-1 | `csvProcessor.js`（LF正規化処理部分） | 改行コードのLF正規化が「データ(ダブルクォーテーション)内の改行も巻き込んで置換してしまうが、そういう仕様として扱う」という注記 | バグではなく「仕様として割り切った」宣言。利用者から見れば意図せず改行位置がずれる可能性があるため、UIのヘルプ文言にもこの仕様を明記すべき（未着手） |

---

## 5. 備考（設計上のトレードオフとして記録、対応不要）
- `makeUserFunc` はユーザー入力コードを `new Function()` で動的実行する設計。ローカル・単独利用のツールである前提では許容範囲だが、他者と共有する用途（プロファイルファイルの配布等）が発生する場合は、実行コードの出所に注意が必要。
