# CSV Processor 開発計画

対象: `241217_csv_processer`（index.html / csvProcessor.js / csvProcessor.css）
作成日: 2026-08-19（最終更新: 2026-08-21）
本ドキュメントは**未対応の残タスクのみ**を管理します。対応が完了した項目・対応不要/対応方針を決定した項目（1-1〜1-5、2-1、2-2、2-3、2-4）は [開発記録.md](./開発記録.md) に移動しました。項番は開発記録.md と通しになっています。

---

## 1. 開発者コメントに記載の既知の問題（残っているもの）

コード冒頭のコメント（3〜24行）に記載されている項目のうち、未対応のもの。対応済みの項目は開発記録.mdを参照。

| 項目 | 現状 | 備考 |
|---|---|---|
| BOMをうまく扱えない | 未対応 | `uInt8ArrayToTextArray`/`encodeText` にBOM除去・付与処理なし |
| 複雑なCSVが読めなくなっている気がする | 要調査 | モード3のラッパー処理ロジック（1578〜1690行）は複雑で、境界条件のテストが不足している可能性 |
| 文字コード自動判定が動いているか怪しい | 要検証 | `Encoding.convert(...,{from:'AUTO'})` に依存、精度検証・テストケース追加が必要 |
| テキスト欄が長いとレイアウト崩れ | 未対応 | `csvProcessor.css` に長文入力時の `max-height`/`overflow-y`/`word-break` 等の制御が不足 |
| tooltip | 一部実装済み | CSSの`.tooltip`/`.tooltipText`は実装済み。未実装機能欄との整合性確認が必要 |
| 列数が合わない行の扱い | 未対応 | プレビュー用サンプルにも「一列多い/少ない例」が含まれるが、特別なエラー処理や警告表示はない |
| UI調整（折りたたみ） | 未対応 | 設定パネルの折りたたみUIなし |
| ディレクトリの書き出し | 未対応（原因特定） | `getWriteableStream` が `outputDirectoryHandle.getFileHandle(name)` を直接呼んでおり、`/` を含むファイル名（サブディレクトリ振り分け）に対応できない |
| プロファイル読込・書き出しの改善・削除機能 | 削除機能は実装済み | `removeProfile`（1887行付近）が既に存在。コメントが古い可能性が高く、要更新。読込・書き出し自体の「改善」内容は要ヒアリング |
| 強制的な処理の打ち切り | 未対応 | 処理中断ボタン・AbortControllerなし。`processAllStream`のwhileループを止める手段がない |

---

## 2. UI上ですでに「未対応」と明示されている機能（レビュー対象外・現状維持でOK）

- ファイル分割出力（`outputSplitCheckbox` / `outputMaxRowsInput`、disabled済み）
- 出力ファイル存在時の上書き/追記の選択（`outputFileExistActionRadiobutton`、disabled済み）

---

## 3. 開発計画（フェーズ案・残タスクのみ）

### フェーズ2：改行コード周り残タスク・BOM対応
- 死んだ変数 `lineBreakRegExp`（モード3で未使用、1408〜1435行付近）の整理
- BOM対応（読み込み時のBOM除去、書き込み時のBOM付与オプション）

### フェーズ3：ドキュメント・UI整合性の是正
- 開発者コメント冒頭のTODOリストを、今回の調査結果を踏まえて更新（プロファイル削除機能など、実装済み項目の削除）
- ★-1: 改行コード正規化がセル内改行も巻き込む仕様について、UIのヘルプ文言に明記する

### フェーズ4：未実装機能の実装（優先度は要相談）
- 列数不一致行の扱い（警告表示 or 自動補正 or エラー停止の選択）
- 処理の強制中断機能（AbortController導入）
- ディレクトリへの振り分け書き出し対応
- UIの折りたたみ対応、長文入力時のレイアウト崩れ修正（CSS）
- 文字コード自動判定の精度検証・テストケース整備
- ★-2: `readingModeView`（処理モード表示）の実装。現状はモード1かモード3のどちらかにしかならない（モード2は当面実装しない方針が確定）ため、その前提で現在の処理モードを表示する機能として仕上げる
- `directoryReloadButton`（フォルダ再読込ボタン）へのクリックイベント実装。現状クリックイベントが一切登録されていない死んだボタン
- 読み込みモード2の実装（当面は見送り。大容量CSVで「セル内に区切り文字を含む」設定時の処理速度が実運用で問題になった場合に再検討。詳細は開発記録.md 2-4参照）

### フェーズ5：技術的負債の整理
- `processAll`/`processCsv`（非ストリーム版一括処理、現在完全にデッドコード）の削除 or 復活方針の決定。`outputFileNameCode`関連など、生きているコードとの乖離が対応のたびに増えているため優先度を上げてもよい

---

## 4. コード内の★マーク一覧（開発者本人による「微妙・未完成」の目印、残っているもの）

コード中の `★` は、開発者自身が「微妙だと思った箇所」「未完成の箇所」を強調するために付けたマーク。旧★-1〜★-3（`csvProcessor`をユーザーコードに渡す件）は、検討の結果対応不要と判断しコードから★を除去したため、この一覧から削除（詳細は開発記録.md参照）。

| # | 該当箇所 | 内容 | 対応状況・方針 |
|---|---|---|---|
| ★-1 | `csvProcessor.js:1438`付近 | 改行コードのLF正規化が「データ(ダブルクォーテーション)内の改行も巻き込んで置換してしまうが、そういう仕様として扱う」という注記 | バグではなく「仕様として割り切った」宣言。利用者から見れば意図せず改行位置がずれる可能性があるため、UIのヘルプ文言にもこの仕様を明記すべき（フェーズ3） |
| ★-2 | `index.html`（`readingModeView`） | `<span id="readingModeView">処理モードを表示</span><!-- ★ -->` | JS側を全文検索したが参照箇所は一切なく、完全に死んだUI要素。現在の処理モードを表示する機能として仕上げるのが望ましい（フェーズ4） |

---

## 5. 備考（設計上のトレードオフとして記録、対応不要）
- `makeUserFunc` はユーザー入力コードを `new Function()` で動的実行する設計。ローカル・単独利用のツールである前提では許容範囲だが、他者と共有する用途（プロファイルファイルの配布等）が発生する場合は、実行コードの出所に注意が必要。
