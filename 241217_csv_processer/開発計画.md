# CSV Processor 開発計画

対象: `241217_csv_processer`（index.html / csvProcessor.js / csvProcessor.css）
作成日: 2026-08-19
本ドキュメントは、コードレビューの結果とコード冒頭の開発者コメント（既知の問題・TODO）を突き合わせて作成した開発計画です。

---

## 1. 優先度High：実害のあるバグ（コードレビューで新規発見）

### 1-1. 出力書込みタイミング「100,000行ごと」が機能しない → ✅ 対応済み（2026-08-19）
- 該当箇所: `csvProcessor.js` `outputToFile` 内の判定（1138〜1144行付近）
- 内容（修正前）: `outputWritingTiming` の分岐判定が `"100"` `"1000"` `"10000"` のみで、`index.html` のプルダウンにある `"100000"`（100,000行ごとに書込）が抜けていた。
- 影響（修正前）: 「100,000行ごとに書込」を選択すると、処理完了までバッファがフラッシュされず、実質「最後に一括書込」と同じ挙動になっていた。
- 対応内容: 判定条件に `(csvProcessor.options.outputWritingTiming == "100000" && ...outputBuffer.length > 100000)` の分岐を追加。

### 1-2. 文字コード変更時にプレビューがエラーになる（開発者コメント済の症状の原因特定） → ✅ 対応済み（2026-08-19）
- 該当箇所: `document.addEventListener('input', ...)` 内の `inputEncodingSelect` 変更処理（137〜143行付近）、`inputMethod`切り替え処理（152〜177行付近）、`loadAndUpdatePreview`（286〜308行付近）
- 内容（修正前）: 単純な「無条件に`loadAndUpdatePreview()`を呼んでいる」だけの問題ではなく、根本には**「直接入力」方式では入力文字コードの選択がそもそも無意味である、という前提が一切考慮されていなかった**という設計漏れがあった。`inputMethod`切り替え処理は`selectFileButton`や`inputCsvTextInput`等の有効/無効は切り替えるが、`inputEncodingSelect`は一切触っておらず、直接入力モードでも選択可能なままになっていた。その結果:
  1. ファイル未選択・直接入力モードのまま文字コードを変更すると、`loadAndUpdatePreview`が無条件に`csvProcessor.inputFiles[0].fileObj`を参照し`TypeError`でクラッシュする。
  2. さらに厄介なケースとして、一度ファイルを読み込んだ後に直接入力へ切り替えてテキストを編集し、そこで文字コードを変更すると、クラッシュせずに古いファイルを再デコードして直接入力欄の内容を**無言で上書き**してしまう（サイレントなデータ消失）。
  3. 加えて、フォルダ選択で検索結果が0件だった場合も`loadAndUpdatePreview()`が無条件に呼ばれており、同じ`TypeError`の別経路になっていた。
- 対応内容:
  - `inputMethod`切り替え時に、`file`/`directory`では`inputEncodingSelect`を有効化、`direct`では無効化するようにし、「直接入力時は文字コード選択が固定（無効）」という仕様をUI上に明示した。
  - `input`イベントハンドラ側でも、現在選択中の`inputMethod`が`file`/`directory`の場合のみ`loadAndUpdatePreview()`を呼ぶようガードを追加（disabled要素の防御に加えた二重の保険）。
  - `loadAndUpdatePreview`自体の先頭にも、`inputFiles`が空または`fileObj`が無い場合は何もせず`updatePreview()`のみ行うガードを追加し、ファイル0件時のクラッシュも解消。
- 副次的に発見した別件（未対応・別管理）: フォルダ入力の「再読込」ボタン（`directoryReloadButton`）にはクリックイベントが一切登録されておらず、押しても何も起きない死んだボタンだった。今回のスコープ外のため、フェーズ4の未実装機能リストに追記。

### 1-3. カスタム改行文字（入力側）が実質未対応 → ✅ 対応済み（2026-08-19）
- 該当箇所: `csvTextToArray` の改行コード判定 switch と正規化ブロック（1408〜1495行付近）
- 内容（修正前）: 呼び出し元は `lineBreakSelect` に `"CUSTOM"` の場合は実際のカスタム文字列を渡しているが、`csvTextToArray` 内の switch は文字列リテラル `'ALL'/'LF'/'CR'/'CRLF'/'CUSTOM'` としか比較していなかった。そのためカスタム文字列はどのcaseにも一致せず `default`（LF扱い）にフォールバックしていた。
- 対応内容: 改行コードの正規化ブロック（ALL/CR/CRLFをLFに一括置換している箇所）に、「既知のキーワードに一致しない＝カスタム改行文字」とみなす分岐を追加。複数文字のカスタム改行にも対応できるよう、1文字ずつ照合してマッチした分だけインデックスを進める実装とした。これにより `splitTextArray` やモード3のセル解析（いずれも正規化後の`\n`だけを見る設計）を変更せずにカスタム改行が機能するようになった。
- 残課題: `lineBreakRegExp`（1408〜1435行）はモード3側で結局参照されず死んでいる変数のままなので、将来的に整理してよい（1-4とは独立の軽微な整理）。

### 1-4. `splitTextArray` の2文字区切り文字処理にpushし忘れのバグ → ✅ 対応済み（2026-08-19）
- 該当箇所: `splitTextArray`（1976〜1986行付近）
- 内容（修正前）: `delimiter.length == 2` の分岐で `cellArray = textArray[i]`（代入）になっており、`cellArray.push(textArray[i])` の誤り。1文字が上書きされ続け、セル内容が欠落する。呼び出し側が現状 `"\n"`（1文字）固定でしか呼んでいないため実害はまだ発生していなかったが、放置すると今後2文字以上の区切り文字で呼び出す変更が入った瞬間に必ず壊れる不良コードだった。
- 対応内容: `cellArray = textArray[i]` を `cellArray.push(textArray[i])` に修正。1文字delimiterの分岐と同じ挙動になるよう揃えた。

### 1-5. 入力配列の破壊的変更の可能性 → ✅ 対応済み（2026-08-21）
- 該当箇所: `csvTextToArray` 冒頭（1415〜1420行付近）
- 内容（修正前）: `csvTextInput` が配列の場合、`csvTextArray = csvTextInput`（参照コピー）となっていた。改行コード変換（ALL/CR/CRLF/カスタム）を行う分岐では新しい配列 `tmpArray` に置き換わるため無害だったが、`lineBreakSelect` が `"LF"` の場合は変換処理がスキップされ `csvTextArray` は元の配列への参照のまま残っていた。その後の先頭行スキップ処理 `csvTextArray.shift()`（1490行付近）が実行されると、**呼び出し元が保持している元配列（ストリーム処理の `csvBuffer` 等）を書き換えてしまう**可能性があった。
- 対応内容: 配列入力時も `csvTextArray = [...csvTextInput]` として必ずコピーを作成するように修正。これで改行コードの選択に関わらず、この関数が呼び出し元の配列を書き換えることはなくなった。

---

## 2. 優先度Medium：ドキュメント不整合・設計上の懸念

### 2-1. ユーザーコード欄の説明とAPI仕様の不一致 → ✅ 対応済み（2026-08-21）
- 内容（修正前）: `makeUserFunc` は `new Function("a,m,f", code)` でユーザーコードを関数化しており、渡されるデータは第1引数 `a` のプロパティ（例: `a.csvArray`）としてアクセスする必要があった。しかし `index.html` の説明文・「ここで使える変数」表は4つのタブ間でも表記がバラバラで、`file` / `csvArray` / `options` 等の裸の変数名がそのまま使えるかのように読める箇所が大半だった。素直に裸の変数名で書くと `ReferenceError` が発生し、`makeUserFunc` 内部の `try/catch` に無言で握りつぶされ、コンソールを見ない限りユーザーは気づけなかった。
- 対応内容（性能面は問題にならないと判断し、裸の変数名で書ける方式を採用）:
  - `makeUserFunc(userCode, varNames)` に第2引数を追加し、`varNames` が指定されていれば `var {…} = a;` という分割代入をユーザーコードの直前に自動挿入するようにした。`var`宣言にしたのは、ユーザーコードが同名のローカル変数を`var`で宣言しても構文エラーにならないようにするため。
  - `csvProcessor.userFuncVars` に、各タブの「ここで使える変数」表と一致する変数名リストを定義（`perInput`/`perRow`/`perCell`/`perOutput`）。
  - `processAllStream` 内の4箇所の関数生成（`perInputFunc`/`perRowFunc`/`perCellFunc`/`perOutputFunc`）に、対応する変数名リストを渡すよう変更。
  - `index.html` の「読込ごとの処理」タブの説明文から `a.csvArray` の表記を削除し、他タブと同じ「csvArray」という裸の表記に統一。
  - 後方互換性: `a` 自体も引き続き第1引数として渡っているため、既存のプロファイル（`a.csvArray` 等の書き方で保存済みのコード）はそのまま動作する。
- 追加対応（2026-08-21）: 上記では`outputFileNameCode`をスコープ外としていたが、ツール全体としての一貫性を優先し追加対応した。
  - `index.html`の「ファイル名(振分・抽出)」欄に、他の設定項目と同じ`.tooltip`形式で「ここで使える変数」の説明を新設（`file, fileObj, csvIndex, csvArray, rowIndex, rowArray, rowText, options, sessionRowCount`）。
  - `csvProcessor.userFuncVars.outputFileName`を追加し、`processAllStream`内の`outputFileNameFunc`生成（797行目付近）にのみ変数名リストを渡すよう修正。dead codeである`processAll`側（473行目）は意図的に変更していない。
  - これで5つのユーザーコード入力欄すべてで「裸の変数名で書ける」が統一された。
- 副次的に発見した別件（未対応・別管理）: 今回の調査で、`processAll`/`processCsv`（非ストリーム版の一括処理関数）が**現在どのUI操作からも呼び出されない完全なデッドコード**であることを確認した（出力ボタンは`processAllStream`のみを呼ぶ）。削除するか、意図的に残すか方針を決める必要がある。

### 2-2. `outputBufferSize`（20MB設定）が未使用のデッドコード
- 該当箇所: `settings.outputBufferSize`（283行）
- 内容: 定義されているが、コード全体のどこからも参照されていない。行数ベースの書込みタイミング制御はあるが、メモリサイズベースの制御は未実装。
- 対応: 実装するか、誤解を避けるため削除する。

### 2-3. `csvProcessor` オブジェクト全体をユーザーコードに渡す設計
- 該当箇所: `perInputFunc`/`perRowFunc`/`perCellFunc` 呼び出し時の引数（926行付近に開発者自身のコメント「★メモリリークするなら入れない」あり）
- 内容: 開発者自身がメモリリークの懸念を認識済み。大量ファイル処理時に長時間実行されるツールのため、要検証。
- 対応: 実際にメモリプロファイリングを行うか、必要な機能（`sessionMemory` 経由のセッション変数保持等）だけを渡す形に絞る。

### 2-4. 読み込みモード2が未実装（コード内に明記）
- 該当箇所: `csvTextToArray` の `readingMode` 分岐（1576〜1578行）
- 内容: 「セル内に区切り文字を含むが改行は含まない」場合用の高速処理（モード2）が未実装で、コメント`// 今作っていないのでレベル3で処理`のとおり常に低速なモード3（1文字ずつ全解析）にフォールバックしている。
- 影響: UIのtooltipで「読み込みに必要な時間が大幅に増加します」と警告している設定を選んでも、実際にはモード2による高速化の恩恵がなく、常に最も遅い処理になる。
- 対応: モード2を実装するか、当面はtooltip文言を実態に合わせて修正する。

---

## 3. 開発者コメントに記載の既知の問題（現状把握・要再確認）

コード冒頭のコメント（3〜24行）に記載されている項目を、コードレビュー結果と突き合わせて再整理。

| 項目 | 現状 | 備考 |
|---|---|---|
| BOMをうまく扱えない | 未対応（現状維持） | `uInt8ArrayToTextArray`/`encodeText` にBOM除去・付与処理なし |
| 複雑なCSVが読めなくなっている気がする | 要調査 | モード3のラッパー処理ロジック（1578〜1690行）は複雑で、境界条件のテストが不足している可能性 |
| プレビューで文字コード選択時にエラー | ✅ **対応済み（2026-08-19）** | 1-2で対応。単なるクラッシュ修正ではなく「直接入力時は文字コード選択が無意味」という設計漏れが根本原因だった |
| 文字コード自動判定が動いているか怪しい | 要検証 | `Encoding.convert(...,{from:'AUTO'})` に依存、精度検証・テストケース追加が必要 |
| テキスト欄が長いとレイアウト崩れ | 未対応 | `csvProcessor.css` に長文入力時の `max-height`/`overflow-y`/`word-break` 等の制御が不足 |
| カスタム改行文字に対応 | ✅ **対応済み（2026-08-19）** | 1-3で修正。なお1-4の`splitTextArray`のバグは今回の修正経路では踏んでいないため、別件の死んだコードとして残存 |
| tooltip | 一部実装済み | CSSの`.tooltip`/`.tooltipText`は実装済み。②未実装機能欄との整合性確認が必要 |
| 列数が合わない行の扱い | 未対応 | プレビュー用サンプルにも「一列多い/少ない例」が含まれるが、特別なエラー処理や警告表示はない |
| UI調整（折りたたみ） | 未対応 | 設定パネルの折りたたみUIなし |
| ディレクトリの書き出し | 未対応（原因特定） | `getWriteableStream` が `outputDirectoryHandle.getFileHandle(name)` を直接呼んでおり、`/` を含むファイル名（サブディレクトリ振り分け）に対応できない |
| プロファイル読込・書き出しの改善・削除機能 | **削除機能は実装済み** | `removeProfile`（1887行）が既に存在。コメントが古い可能性が高く、要更新。読込・書き出し自体の「改善」内容は要ヒアリング |
| 強制的な処理の打ち切り | 未対応 | 処理中断ボタン・AbortControllerなし。`processAllStream`のwhileループを止める手段がない |

---

## 4. UI上ですでに「未対応」と明示されている機能（今回のレビュー対象外・現状維持でOK）

- ファイル分割出力（`outputSplitCheckbox` / `outputMaxRowsInput`、disabled済み）
- 出力ファイル存在時の上書き/追記の選択（`outputFileExistActionRadiobutton`、disabled済み）

---

## 5. 開発計画（フェーズ案）

### フェーズ1：既知バグの修正（優先度High、着手推奨）
1. ✅ 1-1: 出力書込みタイミング「100,000行ごと」判定漏れの修正（対応済み）
2. ✅ 1-2: 文字コード変更時のプレビュークラッシュ修正（対応済み）
3. ✅ 1-5: `csvTextToArray` の配列破壊的変更の修正（対応済み）
4. ✅ 1-4: `splitTextArray` の2文字区切り文字pushバグ修正（対応済み）

### フェーズ2：改行コード周り残タスク・BOM対応
- ✅ カスタム改行文字対応は完了（1-3）
- ✅ `splitTextArray` の2文字delimiterバグは修正済み（1-4）
- 死んだ変数 `lineBreakRegExp`（モード3で未使用）の整理
- BOM対応（読み込み時のBOM除去、書き込み時のBOM付与オプション）

### フェーズ3：ドキュメント・UI整合性の是正
- ✅ 2-1: ユーザーコード欄の変数アクセス方法の説明を実装と一致させる（対応済み。裸の変数名で書ける方式を採用）
- 開発者コメント冒頭のTODOリストを、今回の調査結果を踏まえて更新（プロファイル削除機能など、実装済み項目の削除）
- tooltip文言の実態確認（モード2未実装の影響）
- ✅ `outputFileNameCode`（ファイル名 振分・抽出欄）にも同様の変数説明と`userFuncVars`方式を適用済み（対応済み）

### フェーズ4：未実装機能の実装（優先度は要相談）
- 列数不一致行の扱い（警告表示 or 自動補正 or エラー停止の選択）
- 処理の強制中断機能（AbortController導入）
- ディレクトリへの振り分け書き出し対応
- UIの折りたたみ対応、長文入力時のレイアウト崩れ修正（CSS）
- 文字コード自動判定の精度検証・テストケース整備
- ★-5: `readingModeView`（処理モード表示）の実装。現在どの読み込みモード（1/2/3）で動作しているかを表示する死んだUI要素を、2-4のモード2対応方針決定と合わせて仕上げる
- `directoryReloadButton`（フォルダ再読込ボタン）へのクリックイベント実装。1-2の調査で、現状クリックイベントが一切登録されていない死んだボタンであることが判明

### フェーズ5：技術的負債の整理
- 2-2: `outputBufferSize` の実装 or 削除
- 2-3: メモリリークの検証（大容量ファイルでの長時間処理テスト）
- 読み込みモード2の実装、またはモード3のみに一本化する方針決定
- `processAll`/`processCsv`（非ストリーム版一括処理、現在完全にデッドコード）の削除 or 復活方針の決定

---

## 6. コード内の★マーク一覧（開発者本人による「微妙・未完成」の目印）

コード中の `★` は、開発者自身が「微妙だと思った箇所」「未完成の箇所」を強調するために付けたマーク。プロジェクト全体を検索した結果、以下の5箇所に付与されていた。レビューで見つけたバグとは別に、開発者本人が既に懸念を認識していた箇所として個別に管理する。

| # | 該当箇所 | 内容 | 対応状況・方針 |
|---|---|---|---|
| ★-1 | `csvProcessor.js:926` | `perInputFunc` 呼び出し時、引数オブジェクトに `csvProcessor` 全体を渡している箇所に「メモリリークするなら入れない」というコメント | 2-3と同一の懸念。ストリーム処理中に何度も呼ばれる関数に巨大なオブジェクトを渡し続けているため、要メモリプロファイリング（フェーズ5） |
| ★-2 | `csvProcessor.js:966` | `perRowFunc` 呼び出し時、同上のコメント | 同上（★-1と同じ懸念が3箇所に重複して書かれている＝開発者が繰り返し気にしていた箇所） |
| ★-3 | `csvProcessor.js:998` | `perCellFunc` 呼び出し時、同上のコメント。セルごと＝行内で最も呼び出し頻度が高い処理のため、3箇所の中で最もパフォーマンス・メモリへの影響が大きいと推測される | 同上。優先してここから検証するのが妥当 |
| ★-4 | `csvProcessor.js:1438`（現行行番号、旧1437〜1438行） | 改行コードのLF正規化が「データ(ダブルクォーテーション)内の改行も巻き込んで置換してしまうが、そういう仕様として扱う」という注記 | バグではなく「仕様として割り切った」宣言。ただし利用者から見れば、セル内改行を含むデータで意図せず改行位置がずれる可能性があるため、ドキュメント（UIのヘルプ文言）にもこの仕様を明記すべき（フェーズ3） |
| ★-5 | `index.html:95` | `<span id="readingModeView">処理モードを表示</span><!-- ★ -->` | JS側を全文検索したが `readingModeView` を参照する箇所は一切なく、**完全に死んだUI要素**。おそらく2-4で触れた `readingMode`（1/2/3、処理の重さが変わる内部フラグ）を画面に表示する機能を意図していたが未実装のまま放置されている。2-4のモード2実装 or tooltip文言修正と合わせて、「現在どの処理モードで動いているか」を実際に表示する機能として仕上げるのが望ましい（フェーズ4候補に追加） |

---

## 7. 備考（設計上のトレードオフとして記録、対応不要）
- `makeUserFunc` はユーザー入力コードを `new Function()` で動的実行する設計。ローカル・単独利用のツールである前提では許容範囲だが、他者と共有する用途（プロファイルファイルの配布等）が発生する場合は、実行コードの出所に注意が必要。
